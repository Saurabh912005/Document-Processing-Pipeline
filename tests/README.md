# Integration / E2E tests

Backend integration tests live in `backend/src/test/java` (JUnit 5 + Spring Boot Test + MockMvc).

Frontend component tests live in `frontend/src/**/*.test.jsx` (Vitest + React Testing Library).

Optional end-to-end tests against Docker Compose can be added here (e.g. Playwright) if you extend the project.
